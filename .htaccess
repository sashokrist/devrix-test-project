RewriteEngine On
RewriteBase /devrix-test-project/

# Redirect taxonomy-archive to working handler
RewriteRule ^taxonomy-archive/?$ taxonomy-archive/index.php [QSA,L]

# Custom rewrite rules for students - use the handler file
RewriteRule ^students/([^/]+)/?$ students.php [QSA,L]
RewriteRule ^students/?$ /devrix-test-project/?post_type=student [QSA,L]

# BEGIN WordPress
# The directives (lines) between "BEGIN WordPress" and "END WordPress" are
# dynamically generated, and should only be modified via WordPress filters.
# Any changes to the directives between these markers will be overwritten.

# END WordPress